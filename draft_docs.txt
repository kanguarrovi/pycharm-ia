
This proyect is using uv for pip management.

-> uv pip install fastapi "uvicorn[standard]"

Comand to run:

-> uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

Run tests:
-> pytest # On the root folder